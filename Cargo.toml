[package]
name = "rblue"
version = "0.1.0"
edition = "2024"

[workspace]
members = [
    "host",          # 实际 crate 名会是 rblue-host
    "hci-protocol",  # 实际 crate 名会是 rblue-hci-protocol
    "controller",    # 实际 crate 名会是 rblue-controller
    "phy",           # 实际 crate 名会是 rblue-phy
]
resolver = "2"

[dependencies]
